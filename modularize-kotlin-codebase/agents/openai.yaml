interface:
  display_name: "Modularize Kotlin Codebase"
  short_description: "Orchestrate safe Kotlin modularization"
  default_prompt: "Use $modularize-kotlin-codebase to orchestrate audit, conventions, foundations, feature migration, cycle breaks, platform boundaries, API hardening, verification, and optional build metrics."
