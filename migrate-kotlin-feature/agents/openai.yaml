interface:
  display_name: "Migrate Kotlin Feature"
  short_description: "Move one feature without widening APIs"
  default_prompt: "Use $migrate-kotlin-feature to migrate one vertical feature slice without widening public dependency or native framework boundaries."
