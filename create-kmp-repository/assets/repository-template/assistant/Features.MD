# @@APP_NAME@@ features

## Home

The Home feature is the runnable starter slice and the first app entry point. It demonstrates the domain/data/navigation/UI/test module contract without product-specific behavior.

See `feature/home/Feature.MD` and `assistant/screens/HomeDestination.Landing_help.MD`.

Update this hub only when features or app-wide navigation landmarks are added, removed, or renamed.
