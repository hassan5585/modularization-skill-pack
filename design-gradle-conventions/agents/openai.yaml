interface:
  display_name: "Design Gradle Conventions"
  short_description: "Extract safe Gradle convention plugins"
  default_prompt: "Use $design-gradle-conventions to consolidate repeated Gradle configuration while preserving narrow dependency and native framework surfaces."
